@extends('admin.layout')
@section('title','Tipos de permiso')
@section('page_title','Tipos de permiso')
@section('page_description','Configura las reglas que tendrá cada tipo de solicitud.')
@section('content')
<div class="mb-7 rounded-[2rem] bg-gradient-to-br from-slate-950 via-violet-950 to-fuchsia-900 p-7 text-white shadow-xl"><div class="text-xs font-bold uppercase tracking-[.22em] text-fuchsia-200">Reglas de solicitud</div><h1 class="mt-2 text-3xl font-black">Tipos de permiso</h1><p class="mt-2 text-fuchsia-100">Define qué solicitudes descuentan vacaciones y qué firmas o recepciones requieren.</p></div>
<div class="grid grid-cols-1 gap-6 xl:grid-cols-3"><section class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-xl font-black">Nuevo tipo</h2><form method="POST" action="{{ route('admin.permisos.tipos.store') }}" class="mt-5 space-y-4">@csrf<input name="nombre" class="w-full rounded-xl border-slate-300" placeholder="Nombre" required><input name="slug" class="w-full rounded-xl border-slate-300" placeholder="slug opcional"><textarea name="descripcion" rows="3" class="w-full rounded-xl border-slate-300" placeholder="Descripción"></textarea>@foreach(['descuenta_vacaciones'=>'Descuenta vacaciones','requiere_saldo'=>'Requiere saldo','requiere_firma_colaborador'=>'Firma colaborador','requiere_firma_lider'=>'Firma líder','requiere_recepcion_rh'=>'Recepción RH','activo'=>'Activo'] as $campo=>$etiqueta)<label class="flex items-center justify-between rounded-xl border border-slate-200 px-3 py-2 text-sm font-semibold"><span>{{ $etiqueta }}</span><input type="checkbox" name="{{ $campo }}" value="1" @checked(in_array($campo,['requiere_firma_colaborador','requiere_firma_lider','requiere_recepcion_rh','activo']))></label>@endforeach<button class="w-full rounded-xl bg-slate-950 px-5 py-3 font-black text-white">Guardar tipo</button></form></section><section class="space-y-4 xl:col-span-2">@foreach($tipos as $t)<form id="tipo-{{ $t->id }}" method="POST" action="{{ route('admin.permisos.tipos.update',$t) }}" class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm">@csrf @method('PUT')<div class="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"><div class="grid flex-1 grid-cols-1 gap-3 md:grid-cols-2"><input name="nombre" value="{{ $t->nombre }}" class="rounded-xl border-slate-300 font-bold"><input name="slug" value="{{ $t->slug }}" class="rounded-xl border-slate-300"><input name="descripcion" value="{{ $t->descripcion }}" class="rounded-xl border-slate-300 md:col-span-2"></div><button class="rounded-xl bg-slate-950 px-4 py-2.5 font-bold text-white">Guardar</button></div><div class="mt-4 grid grid-cols-2 gap-2 md:grid-cols-3">@foreach(['descuenta_vacaciones'=>'Descuenta vacaciones','requiere_saldo'=>'Requiere saldo','requiere_firma_colaborador'=>'Firma colaborador','requiere_firma_lider'=>'Firma líder','requiere_recepcion_rh'=>'Recepción RH','activo'=>'Activo'] as $campo=>$etiqueta)<label class="cursor-pointer"><input type="checkbox" name="{{ $campo }}" value="1" class="peer sr-only" @checked($t->$campo)><span class="flex min-h-11 items-center justify-center rounded-xl border border-slate-200 bg-slate-50 px-3 text-center text-xs font-bold text-slate-500 peer-checked:border-violet-500 peer-checked:bg-violet-600 peer-checked:text-white">{{ $etiqueta }}</span></label>@endforeach</div></form>@endforeach<div class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm">{{ $tipos->links('vendor.pagination.rh') }}</div></section></div>
@endsection
